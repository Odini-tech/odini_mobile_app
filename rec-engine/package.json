{
  "name": "odini-rec-engine",
  "version": "0.2.0",
  "description": "Odini smart engine (rec_eng) — built independently against the current /api/* contract in recommendationGateway.ts. Adds Ask Odini (RAG) on top; feed/explore/mixes/similar are placeholder logic pending reconciliation with the team's separate recommendation-engine repo.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
